# Section 08 redesign: efficient and adapted LLMs

One GPT-6 Astra High owner implemented chapters 40–46 and `labs/s08-adaptation`; original projects remain unchanged. Full original lessons, metadata, research, reference projects and the supplied technical/pedagogy reports were read. Original section anchors are preserved. Tagged excerpts refer to the new labs or retained complete reference projects; retired micro-exercise links have been replaced. No new dependency beyond the existing Chapter 36 path library. No commit, push or publication.

The package has seven useful learner baselines and seven separate explained solutions. `NN`, `NN --check`, `NN --solution`, `NN --solution --check` are the public CLI. Each learner comparison failure exits 1 with `GOAL_NOT_MET:`; ordinary argument/runtime validation is not relabeled. There are no TODO panics or failing ordinary tests. Exact source-edit controls are named in each lesson; Chapter 40 has the independent `--stages` diagnostic, and Chapters 41/42 have the `--bench` flag. Implementation and transfer occur in every session; estimates are 30–45 minutes per session, not per chapter.

| Chapter / original topics | Objectives and step mapping | Meaningful learner change | Check, actual values and unfamiliar transfer | Sessions | Prerequisite audit / limits |
|---|---|---|---|---|---|
| 40 KV cache; sampling; batching; streaming | `cache`, `implementation`, `sampling`, `serving`, `experiment` | Complete three bounded embedding/append/attention stages using supplied residual blocks, logits and incremental orchestration; route the full-prefix baseline to the staged decoder; replace greedy sampling and serial requests with top-k/temperature sampling and round robin | Six unfamiliar prefixes agree; 192 cached scalars and 12 layer-token projection rows versus baseline 42; quotas [1,3,2] emit a,bb,c,bb,c,bb; seeded top-k excludes third candidate | 2 × 40 min | Retrieves causal masking, QKV, learned positions and stable softmax from 35–39. Random decoder proves equivalence, not language quality. Scalar event scheduler has no networking/fused batching. |
| 41 Int8; int4; dequantization; quality and speed | `mapping`, `packing`, `quality`, `speed`, `practice` | Replace global int8 encoding with row-specific int8/packed int4 and signed decoding; inspect integer accumulation and model boundary | Unseen 4×9 storage 144/52/34 bytes; signed odd-tail roundtrip; int32 dot 48,387; decoder loss f32 2.500316, int8 2.500288, int4 2.501305; both argmax 8 despite different logit error | 38+41 min | Retrieves row-major dense layout and loss. Explicit [D,V]↔[V,D] transpose. Packed scalar kernel is weight-only; integer-only trace is separate. Output projection only, random model. |
| 42 Online softmax; tiled attention; grouped-query attention; context costs | `online`, `tiles`, `gqa`, `experiment` | Replace scalar row workspace with complete persistent m/l/o tile merge, causal indexing and GQA output | Seven positions, tiles 1/2/4/9, every-coordinate oracle agreement; actual peak score vector measured where allocated; shifted scores 1001/1003/1002 retain output 0.493734 | 42+40 min | Retrieves stable normalization, head layout and causal mask. GQA groups query heads; arithmetic remains quadratic. Oracle already stores one row; no GPU/backward claims. |
| 43 Supervised fine-tuning; LoRA; distillation | `response-mask`, `lora`, `adapter-build`, `distillation`, `practice` | Select masked response gradients through prefix differences; train both LoRA factors from old state; implement temperature-consistent soft gradients | Response loss 1.9059→0.0077; unseen-prefix 1.9057→0.0077. Tau=2 transfer KL 0.74459→0.01597, unseen tokens 0.47346→0.23136. Mask invariance, nonzero A derivative, frozen base and direct/merged parity pass; generic retention also printed | 40+40+43 min | Retrieves next-token CE, chain rule and projection layout. Full backwards/cloned effective model and V hard-target backwards favor clarity over memory efficiency. Nearby held-out fixtures do not prove broad transfer. |
| 44 Retrieval baselines; embeddings; indexing; grounded evaluation | `lexical`, `dense`, `dense-build`, `context`, `practice` | Implement complete BM25, exact inner-product search (not L2), source context and selective-risk denominator | Dense loss 1.0984→0.6028, held-out hit@1 3/3. Four-query answering: coverage 3/4, supported sources 2/2, unsupported abstention 1/2. Familiar-word unsupported query actually fails. Threshold 1 gives coverage 0 and risk None | 40+40+43 min | Retrieves embeddings/dot products and held-out evaluation. Defines raw ranking vs support probability. Three documents, fixed vocabulary, extractive reader; citation provenance is not relevance or entailment. |
| 45 Bandits; tabular control; policy gradients (including PPO) | `bandits`, `bandit-build`, `q-learning`, `control-build`, `policy-gradient`, `ppo` | Replace round-robin collection and immediate-reward estimates with epsilon-greedy/Q-learning; build clipped PPO batch reuse | Q right values [0.800325,0.8635,0.93,1]; both clipping signs; PPO better action probability 0.991312. Five-seed regret: epsilon .1 gives [130.4,128.8,152.4,164.1,159.7], epsilon 0 gives 2800 each | 40+40+44 min | Retrieves probability/logit/update distinctions; introduces interaction, return and off-policy bootstrap before code. Finite stationary simulations; PPO has no critic/GAE/decoder RLHF. |
| 46 DPO; reference policy; preference pairs; verifiable reward (plus learned reward models) | `reward-model`, `reward-build`, `dpo`, `reference`, `practice`, `verifiable`, `transfer` | Replace average reward features with Bradley–Terry training, chosen-only supervision with full DPO gradients, and winner-target training with exact reward gradients | DPO numerical slope/order checks; expected-reward first logits [-1/18,1/9,-1/18]; correct final probability >.99. Balanced reward weights [3.6288,0], confounded weights [2.1744,2.1744]; wrong-long score 4.3489. Opposed pairs retain ln2 loss | 40+40+41 min | Retrieves policy gradients and stable log probability. Pair comparisons do not certify correctness. Categorical fixtures, incomplete equality verifier, no hard DPO KL bound or full LM alignment claim. |

## Sources and author decisions

Primary sources remain in each chapter’s metadata and research record: PagedAttention/Orca (40); Jacob et al./GPTQ and format documentation (41); online normalizer/FlashAttention/GQA (42); LoRA/distillation/FLAN/InstructGPT (43); BM25/DPR/RAG/ALCE/selective QA (44); final Sutton–Barto, Watkins–Dayan, Williams and PPO (45); DPO, Williams and InstructGPT (46). `1707.06347` is correctly PPO. InstructGPT full-pipeline evaluations are not attributed to SFT alone. Old research records are explicitly historical where new implementation scope supersedes them. Learning-science guidance motivates retrieval, worked values, staged implementation and transfer; no study is claimed to validate this exact Rust course.

## Interactive and verification

One browser tool, home Chapter 40 `inference-budget`: two layers, width eight, f32 KV, five positions → 640 cache bytes, 30 recomputed versus 10 cached layer-token rows. Four weights [-1,-.3,.2,1]: int4 codes [-7,-2,1,7], six bytes including scale, max error .057142857; int8 [-127,-38,25,127], eight bytes, max error .003149606. Square score tensor covers both heads; row/tile workspace is explicitly one query/head. Six positions → 768 bytes, 42 versus 12 rows. Native number/select/reset controls, deterministic reset, no autoplay/network, SVG title/description, static and noscript equivalents. It illustrates arithmetic; it never runs Rust or records mastery.

Executed on the final package: `cargo fmt --check`; `cargo clippy --all-targets -- -D warnings`; `cargo test` (43 passing); all seven baseline CLI runs (exit 0); all seven solution goal checks (exit 0); all seven learner goal checks (expected exit 1, prefix verified). `node labs/s08-adaptation/test-demo.cjs` verifies the shared browser arithmetic and invalid controls. Scoped HTML checks verified ordered steps, matching metadata, exact topic keys, original anchors and exact current source excerpts, existing source links and 30–45-minute session totals. Runtime outputs are retained in `guidance/validation-redesign/section-08-author.json`.

Optional release measurements actually run: AMD Ryzen 9 9900X, x86_64 Linux, rustc 1.96.0, one thread, five warmups/seven samples. Chapter 41, 200 allocating 256×256 matvecs: f32 median 4.567315 ms (4.541226–4.663214), int8 6.626393 ms (6.623523–6.628112), int4 9.340646 ms (9.327096–9.398985). Smaller packed storage was slower here. Chapter 42, T128/Hq8/Hkv2/d16/tile16: scalar 1.177376 ms (1.174075–1.195185), tiled .927960 ms (.921511–1.019399), max error 1.2e−7. These are allocating end-to-end local function times, not allocation-free kernels or portable speed claims.

Chapter 40 final scaffold audit: `--stages` checks position-two embeddings, preserved two-row K/V state, a hand-computable equal-score attention mean, and three-prefix incremental parity independently. The supplied learner reports errors .03640460 / one retained row / 1.0 mean error / .06758814 prefix error; reference stages pass with zero errors, two retained rows, 96 scalars and six actual projection rows. The full final goal uses six unfamiliar prefixes.
