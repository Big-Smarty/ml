# Astra High review — Chapters 47–51

Reviewed and corrected 2026-09-08. Ownership: GPT-6 Astra High; prior drafts: Sol High. Bounded independent research/technical verification: GPT-5.6 Luna High subagent `verify_math`, read-only. Research routes and primary-source evidence are recorded in each chapter's research.md. Read AUTHORING, template, numerics, review contract, course.json, the complete Chapter 1 lecture and reference implementation, and every assigned lesson/meta/research/reference/starter/exercise/solution. Applied Rust coding and Rust-pattern skills, with standard-library implementations and chapter-local gates.

## Changes and evidence

| Chapter | Review outcome and corrections | Remaining scope limits |
|---|---|---|
| 47 | Magnitude selection and CSR indexing correct. Added direct expected-output checks for both timed allocation-free kernels, storage accounting (3×3 CSR is larger than dense; 192×192 at K=7,373 costs 119,512 versus 294,912 payload bytes), and nonmonotonic output-error explanation. Starter now preserves the actual dense oracle. | Untrained fixed matrix; one input probe; scalar CPU timings exclude conversion and allocation; no sparse retraining or task-accuracy claim. |
| 48 | Full-softmax selected gate, task derivative, capacity accounting and Switch auxiliary derivative correct. Added all-router-weight finite differences with balancing active and hard frequencies held fixed. Added valid two-expert auxiliary value 0.885, showing balanced value one is not universal minimum. Minimum-one integer capacity explicitly project-specific. Starter exposes stable router probabilities. | Top-1 boundaries nonsmooth; uncapped printed task loss excludes drops/auxiliary; tiny CPU layer has no scaling evidence. |
| 49 | Affine composition, inclusive scan, and feature-kernel attention verified. Added finite-overflow rejection and promised nonfinite-input tests. Explained vector-summary shapes, output versus streaming memory, training-state costs and compression tradeoffs. Starter preserves a direct recurrence. | Fixed untrained selective coefficients; sequential O(N log N) scan; ReLU+1 kernel differs from softmax; timings single-run illustrative. |
| 50 | Real threaded data/tensor forward/backward and learned two-stage pipeline verified against serial. Added first-stage finite-difference chain-rule check; version, length, parse, nonfinite, cursor and step-overflow rejection checks. Added worked pipeline backward arithmetic. Corrected recovery language: same-process string round trip, not disk persistence or relaunch; unchanged external configuration required. | No multi-GPU, network collective, worker failure detection, durable checkpoint writes, or process restart. |
| 51 | VAE, GAN, diffusion training formulas and corrected DDIM path verified. Added common-seed 512-sample summaries for all models (previous default omitted GAN draws), full Gaussian VAE observations versus explicitly labelled decoder means, finite-grid expectation and gradient checks, nonzero-log-variance starter/exercise checks. Explained fixed noise variance 0.6567 and that all three affine Gaussian samplers cannot fit two modes. | Finite-difference tiny models; coarse fixed noise grid differs from normal sampling; four-step deterministic DDIM; metrics compare to training fixture, not held-out distribution quality. |

## Scoped validation

All five reference packages: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, and release-mode default runs pass. Each starter passes fmt, strict all-target clippy, and release default run; each starter test run fails at the intended `todo!`. Each Rustlings exercise compiles and intentionally fails; every solution test binary passes. Literal `// TODO` and newline-after-`#[test]` are present. No combined Rustlings/shared course files were edited.

Reference test counts: ch47=3, ch48=4, ch49=4, ch50=3, ch51=4. All lessons have unique ids, valid metadata JSON, and at least 2,000 whitespace-counted meaningful words (47=2008, 48=2056, 49=2066, 50=2007, 51=2085). Exact command outputs are in `/tmp/astra-chNN-final-gates.log`; the lead owns integrated build/link/browser validation.

Measured fixture outcomes: ch48 mean half-squared task loss 1.36810 → 0.00222, route counts [18,18,18], overflow zero. Ch50 tensor/pipeline update parity and recovery equivalence true. Ch51 beta-VAE 0.4521 → 0.1666 and diffusion noise MSE 0.6567 → 0.4190. Its generated central counts are nonzero for every sampler while the six-point target has none, illustrating why improving surrogate losses do not establish distribution fit. Sparse timings varied by density and are not generalized to other hardware.

## Primary-source verifier conclusions

Luna verified Switch equation 3 and auxiliary formula, S4/linear-attention recurrence, AEVB reparameterization, GAN non-saturating loss, and DDIM equation 12 against original primary sources. Its sole must-fix finding was attributing the minimum-one capacity rule too generally; integrated as a project convention. Other explicit limitations were retained rather than misrepresented as faithful large-scale architectures.
