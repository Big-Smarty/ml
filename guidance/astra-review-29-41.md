# Astra High independent review: Chapters 29–41

Review date: 2026-09-08. Owner: GPT-6 Astra High. Status: complete; all scoped gates pass. No shared asset, build, course manifest, or global validation record was edited. Root owns integration and hardware gates.

## Review and research route

Read the authoring, numerical, template, and review guidance, course manifest, full Chapter 01 lesson/reference, and Rust skills. Independently read all thirteen lectures, metadata and research notes, full reference Rust/WGSL, tests, starters, Rustlings exercises/solutions, local corpus fixtures, and manifests. Preserved the existing full implementations and the Chapter 35 interactive demo. No Chapter 36 public interface or shader changed.

Three bounded read-only GPT-5.6 Luna High agents verified primary sources and technical claims: GPU 29–32; language/tokenizer/attention/corpus 33–37; optimizer/training/serving/quantization 38–41. Astra assessed every proposed finding. A claimed Chapter 34 comparator bug was rejected after a minimal executed Rust comparator check; the existing reverse comparator correctly selects the lexicographically smaller tied pair. A second Luna pass verified all newly written attention gradients, decoder counts, GELU derivative, and stable-loss grouping without finding a mathematical error.

Sources checked include original Transformer, GELU, LayerNorm, Adam, AdamW, SGDR, PagedAttention, Orca, quantization and corpus papers; official wgpu v29/WGSL/Vulkan references; official PyTorch, Hugging Face, TorchAO and LiteRT documentation. Chapter research notes retain exact primary links. The GELU original was added explicitly to Chapter 36 metadata and the lesson source list.

## Per-chapter findings and corrections

| Chapter | Findings and implemented corrections | Verification and limits |
|---|---|---|
| 29 | Replaced the starter's deliberate wrong arithmetic with an explicit TODO while retaining successful CPU vector addition. Added NaN rejection before adapter lookup; aligned starter instructions. | CPU boundary test passes; GPU kernel untouched. Prior root Radeon vector-add/67-element proof retained. |
| 30 | Corrected stale research stating reduction stopped at 256 elements and library description calling the public API one-workgroup. It stages groups loading up to 512 values each. Added an independently hand-computed rectangular matrix product. Starter now has explicit TODO and working CPU multiplication. | Two CPU tests pass; odd matrix/tiled reduction GPU implementation unchanged. Prior root 777-element staged reduction and odd GEMM hardware proof retained. |
| 31 | Added hidden-bias index 8 to the finite-difference fixture, which now covers input weights, hidden biases, output weights, and output bias. Starter retains successful sigmoid forward before its derivative TODO. | Three CPU tests pass, including nonlinear XOR fitting and stable extreme-logit loss. Device-resident shaders unchanged; prior 800-step Radeon loss decrease and 80-step CPU parameter parity retained. |
| 32 | Clarified that a failed timed output aborts the complete benchmark before summaries, rather than filtering individual samples. Starter traffic accounting uses explicit TODO. | CPU tests and starter gates pass. Timestamp/f16 paths and seven-sample hardware checks unchanged. Root separately formatted the Rustlings solution for its official edition-2024 runner; this review did not overwrite it. |
| 33 | Fixed cancellation in categorical loss by grouping the common-offset subtraction before adding the log-normalizer. Added equal-logits-at-1e8 regression requiring log(256), while retaining embedding/output finite differences and training evidence. | Four CPU tests and release default pass. This is a byte bigram embedding predictor, not a contextual decoder. |
| 34 | Verified correct deterministic BPE tie comparison and added an explicit banana tie test. Added a saved-file sentinel check proving failed exclusive creation preserves the existing tokenizer. | Four tests, serialization and multilingual/default round trips pass. Current save has no temporary-file cleanup ownership bug. Decoder/file expansion bounds remain intact. |
| 35 | Added a complete numerical backward derivation from the two-position fixture through dV, dA, dS, dQ and dK, with causal single-key derivative and flat-index reasoning. Added probability-row normalization checks. Scoped finite-result prose to bounded fixtures. | Full Q/K/V finite differences and future-invariance tests pass. Luna independently recomputed 4ab=0.8847241 and 4ab/sqrt(2)=0.6255944. Lesson exceeds 2,000 words; root demo untouched. |
| 36 | Fixed large-common-offset cross-entropy cancellation. Forward rejects nonfinite logits and loss-only rejects nonfinite loss. Added large-offset, tiny-positive-temperature, and overflowing raw-parameter fixtures. Added exact parameter-count derivation, 237-parameter worked configuration, explicit caller target-shift contract, implemented GELU derivative and original paper. | Nine CPU tests pass, including nontrivial multilayer/projection/norm checks. Strict GPU-feature clippy and ordinary tests pass. Root subsequently reran actual Radeon full logits/all gradients/three updates parity successfully in `guidance/validation/final-gpu-decoder.json`. Public API, spans, GPU shaders unchanged. Lesson exceeds 2,000 words. |
| 37 | Fixed CRLF document separators. Retained complete removed-document title/license/source/cleaned-byte provenance instead of only IDs. Added Windows-newline, removed-provenance, near-similarity and distinct-short-text fixtures. Distinguished current whole-document audit from raw archival, rejection ledgers, substring and semantic detectors. | Four tests and default audit pass. Quadratic ordered dedup and noncryptographic content IDs remain explicit limits. No claim of semantic decontamination. |
| 38 | Fixed schedule endpoint when warmup_steps equals total_steps. Strengthened exact-resume checks with moments, step, data fingerprint and next rate; added existing temporary-file sentinel, negative-second-moment and trailing-byte rejection checks. Corrected storage/throughput/fsync prose and metadata. Added complete accumulation/clipping/Adam worked update and explicit recovery caller-configuration boundary. | Five tests, strict clippy and default save/reload run pass. Existing save already owns a temp only after successful create_new; regression proves another writer's temp survives. File is synchronized, parent directory is not; no checksum. Caller must reuse microbatch length/count. Lesson exceeds 2,000 words. |
| 39 | Validation now rejects zero context/budget and incompatible context. Added unequal final-block token-weighted oracle and asymmetric 32-byte-overlap fixture. Corrected default command's save/load claim and semantic-audit metadata. Added blockwise-vs-sliding-history explanation, weighted example and exact smoke-budget limits. | Two tests and default release training/generation pass. Prior root exact 14,442,496-parameter CPU/GPU training and resume evidence retained; no large rerun. Lesson exceeds 2,000 words. |
| 40 | Corrected review answer implying the implementation automatically rebuilt an exhausted cache: it returns an error and caller replay is required. | Four tests/default pass: every-prefix full-logit equivalence, cache size, top-k, limit and unequal-request event ordering. Fixed initial requests, scalar round-robin scheduling and immediate emissions remain honestly scoped; no new arrivals or tensor batching. |
| 41 | Clarified signed storage range -8..7 versus symmetric emitted range -7..7. Benchmark now passes each complete output vector to the optimization barrier rather than exposing only its first scalar; allocation inclusion disclosed. Matvec rejects nonfinite output; finite-input overflow regression added. | Three tests, final strict clippy, release default and optional seven-sample benchmark pass. 3x6 sizes 72/30/21 bytes; RMSE int8 0.016789, int4 0.143561. Model experiment dequantizes only actual output weights into a dense decoder, clearly distinguished from standalone packed-kernel timing. |

## Executed gates

For every Chapter 29–41 reference and starter: scoped `cargo fmt`, `cargo clippy --offline --all-targets -- -D warnings`, and `cargo test --offline`. All reference CPU tests pass; hardware tests stay explicitly ignored in ordinary runs. All starters compile cleanly, run a meaningful preceding checkpoint successfully, and fail their tests at the intended TODO (exit 101). Reference release defaults 33–41 all pass. GPU reference defaults 29–32 were not needlessly rerun; existing root hardware results cover unchanged shaders and pipelines.

For all thirteen matching Rustlings exercises and solutions: direct `rustc --edition=2024 --test` compilation. Exercises compile and fail at TODO; solutions pass. Literal `// TODO` and newline-separated test attributes preserved. Root reports all 56 exercises also pass its official Rustlings development check.

Chapter 36 additionally passes GPU-feature all-target strict clippy and tests (nine CPU tests, hardware parity test explicitly ignored in this local gate). Root's final Radeon rerun after the loss correction passed. Chapter 41 was regated after its final code correction, including default and optional benchmark.

Detailed transient gate output: `/tmp/astra-ch29-review-gates.log` through `/tmp/astra-ch41-review-gates.log`, with structured companion JSON. Later explicit Chapter 41 and GPU-feature Chapter 36 checks are recorded in this review's tool execution and root integration. Final root integrated checks are authoritative durable global evidence.

The Chapter 41 local release benchmark measured medians for 200 matrix-vector calls: fp32 4.574556 ms, int8 6.644365 ms, int4 10.05068 ms. These are allocation-including scalar kernels on the validation host, not a speed promise; the lesson reports no universal timing claim.

## Final edit scope

- `chapters/{29,30,32,35,36,37,38,39,40,41}/lesson.html`; `chapters/{36,38,39}/meta.json`; `chapters/29..41/research.md`.
- Reference Rust: `projects/ch29/src/main.rs`, `ch30/src/lib.rs`, `ch31/src/main.rs`, `ch33/src/main.rs`, `ch34/src/main.rs`, `ch35/src/main.rs`, `ch36/src/lib.rs`, `ch37/src/main.rs`, `ch38/src/lib.rs`, `ch39/src/main.rs`, `ch41/src/main.rs`.
- Starters 29–32 `src/main.rs` now explicit TODO checkpoints. Scoped formatting may normalize other already-owned Rust formatting, but no algorithms outside the listed reference changes were replaced.
- This report. No shared assets, global build scripts or global validation files changed.
