# Chapter 26 consistency record

## Changes and interface mapping

- Added the early continuity section linking Chapters 25 and 9 and `/conventions.html`.
- Kept GEMM as A `[m,k]` × B `[k,n]` = C `[m,n]`. Logical indices are now `row`, `col`, and `inner` before the first kernel; the code uses those names throughout.
- Renamed the checked reference kernels to `matmul_scalar_row_col_inner`, `matmul_scalar_row_inner_col`, and `matmul_blocked_row_inner_col`. Their argument order and `Result<(), String>` boundary are unchanged.
- Mapped Chapter 25 dense storage explicitly: A is `inputs`, `m=batch`, `k=in_features`, `n=out_features`, and contiguous GEMM B is the explicit transpose `b[inner*n+col] = weights[col*k+inner]` of dense weights `[out_features,in_features]`. A focused test verifies this map.
- Root integration aligned starter, exercise, solution and reference to the identical checked `matmul_scalar_row_inner_col(a,b,c,m,k,n) -> Result<(),String>` interface. Supplied shape validation and output reset leave only the traversal as the exercise. The starter also carries the exact reference `matmul_scalar_row_col_inner` body.
- Kept C caller-owned and allocated once. Every checked kernel validates its provided output length and resets the reused buffer exactly once per call.
- Removed Chapter 26's duplicate `row-major` metadata definition; Chapter 9 remains the canonical glossary owner.

## Shapes and numerical convention

- A, B, and C are contiguous row-major `f32` slices with exact shapes `[m,k]`, `[k,n]`, and `[m,n]`.
- Every output is a sum over the shared `inner` axis of length `k`; there is no loss or data reduction.
- The two scalar traversals and the one-level scalar blocked traversal compare against the same `f64` accumulator with an absolute-plus-relative tolerance. Output allocation remains outside timing; validation and clearing remain inside timing.

## Code and prose checks

- Read the full Chapter 26 lesson, metadata, research note, reference, starter, exercise, and solution; the authoritative consistency, authoring, and numerical guidance; Chapter 9's current interface; and Chapter 25's final interface from its author.
- Searched the repository for every old and new Chapter 26 kernel name. All callers are confined to Chapter 26's independent reference, starter, lesson, and exercise pair; no external code caller requires migration.
- Parsed `chapters/26/meta.json`, checked the required continuity/conventions links, and verified all three `data-source` snippets against `projects/ch26/src/main.rs` with the course consistency checker.
- Checked the owned diff for whitespace errors and stale `i`/`j`/`p`, `ii`/`jj`/`pp`, and old kernel names. The traditional i-j-k/i-k-j notation remains only after its explicit row/col/inner mapping.

## Validation

Baseline before edits:

- Reference format, strict offline Clippy, 2 tests, and default demo passed.
- Starter format, strict offline Clippy, and default demo passed; its single test failed only at the intentional `matmul_ikj` TODO.
- Exercise and solution format/strict offline Clippy passed; the guided exercise failed only at its intentional TODO; the solution test and demo passed.

After edits:

- `cargo fmt --manifest-path projects/ch26/Cargo.toml --check` and strict offline Clippy passed.
- Reference tests passed, 3/3, including odd rectangular blocks, errors, and the dense-weight transpose map. The default demo passed and preserved its dimensions, checksum, timing scope, and `ijk`/`ikj`/`blocked` report labels.
- Starter format, strict offline Clippy, and default demo passed with `[22.0, 28.0, 49.0, 64.0]`; its test failed only at the renamed intentional row-inner-col TODO.
- Both exercise files passed `rustfmt --check` and strict offline Clippy. The guided exercise test and demo failed only at its intentional TODO; the solution test passed 1/1 and its demo printed `[22.0, 28.0, 49.0, 64.0]`.
- `git diff --check` passed for every owned Chapter 26 path.

## Preserved work and limits

- Preserved the algorithms, deterministic fixtures, block sizes, tolerances, benchmark dimensions/repetitions, `--large` CLI, error messages, measured checksum, and starter/exercise TODO state. No pre-existing learner edits existed in the owned files.
- The implementation remains scalar, single-threaded, and one-level blocked. It omits packed panels, register microkernels, multilevel tiling, bias fusion, and timing of the dense-weight transpose. Block 32 remains a calibration value rather than a portable optimum. No long benchmark or GPU run was performed.
- No optional feature compilation or unresolved Chapter 26 issue remains.

Root review: the first row-col-inner implementation block now quotes the actual checked scalar function, including its local accumulator, rather than a silently different output-accumulating loop.

Root integration also removed an avoidable unchecked-versus-checked exercise interface difference. Numerical loops, examples, and intended TODO state are preserved; integrated scoped gates were repeated after this change.
