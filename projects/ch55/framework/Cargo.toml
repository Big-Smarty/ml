[package]
name = "ch55-burn"
version = "0.1.0"
edition = "2021"
rust-version = "1.96"

[dependencies]
burn = { version = "=0.21.0", default-features = false, features = ["std", "ndarray", "autodiff"] }

[workspace]
