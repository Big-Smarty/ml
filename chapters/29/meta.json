{
  "outcomes": [
    "Trace f32 values through one supplied wgpu upload, bind, dispatch and synchronized readback route",
    "Replace a serial chunk shader with bounded per-element SAXPY invocations",
    "Query and report hardware Vulkan adapters without accepting CPU software fallback",
    "Verify positive/negative scale and odd-length CPU/GPU results with an explicit tolerance"
  ],
  "lessons": [
    "What answer should leave the GPU?",
    "How do five numbers become shader inputs?",
    "Who owns the last three values?",
    "Build the scaled vector operation",
    "Which device will actually execute it?",
    "When is a GPU result safe to read?",
    "Verify the real round trip",
    "Explain the result without the code open"
  ],
  "terms": {
    "adapter": {
      "name": "adapter",
      "definition": "A wgpu handle describing one available physical or software GPU implementation.",
      "explanation": "An adapter is the capability-discovery stage before a logical device is created. It reports a name, backend, device type, driver, limits, and optional features. A machine may expose several adapters, including an integrated GPU, a discrete GPU, or a CPU software implementation. Selecting one is part of experimental provenance: results only mean what you think they mean when the program prints which adapter and backend actually ran."
    },
    "compute-shader": {
      "name": "compute shader",
      "definition": "A GPU program launched over numbered invocations without drawing pixels.",
      "explanation": "A compute shader expresses one small operation that many GPU invocations execute. In this chapter the actual WGSL compute kernel is the add entry point, and each invocation adds one pair of vector elements. This use of kernel means GPU program; Chapter 16's statistical kernel K(a,b) is a similarity function with a different owning context. The Rust host still owns validation, buffer creation, binding, dispatch, and synchronization."
    },
    "storage-buffer": {
      "name": "storage buffer",
      "definition": "A GPU-visible byte allocation exposed to a shader for indexed reads or writes.",
      "explanation": "A storage buffer carries bulk values such as activations, weights, or outputs. The shader interprets its bytes according to a declared WGSL element type, while Rust uploads matching bytes. Usage flags are contracts: an input used by a shader needs STORAGE, a result copied out needs COPY_SRC, and a staging destination needs COPY_DST plus MAP_READ. Incorrect flags are validation errors rather than harmless hints."
    },
    "workgroup": {
      "name": "workgroup",
      "definition": "A fixed-size group of shader invocations scheduled together and identified within a dispatch grid.",
      "explanation": "The chapter uses workgroups of 64 invocations. Rust computes dispatch_count(element_count, workgroup_size) with ceiling division, so two workgroups launch 128 invocation IDs for 65 elements. The WGSL guard compares each ID with dispatch_params.element_count before indexing. Workgroup size is declared in WGSL, while workgroup count is chosen by Rust. Later chapters use the fact that invocations in one workgroup can share fast temporary memory and synchronize at barriers."
    },
    "readback": {
      "name": "readback",
      "definition": "The synchronized copy and mapping that makes GPU-produced bytes visible to CPU code.",
      "explanation": "GPU storage memory is not treated like an ordinary Rust slice. The program first copies output into a buffer created for MAP_READ, requests asynchronous mapping, and drives device progress until the callback completes. Only then may it view the bytes as f32 values. Mapping every intermediate result is costly and destroys device residency, so readback is best reserved for required outputs, diagnostics, or occasional metrics."
    }
  },
  "sources": [
    {
      "title": "wgpu 29 standalone compute example",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/examples/standalone/01_hello_compute/src/main.rs",
      "note": "First-party buffer, dispatch, and mapping pattern."
    },
    {
      "title": "wgpu 29 Instance API",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/wgpu/src/api/instance.rs",
      "note": "First-party adapter enumeration API verified against installed source."
    },
    {
      "title": "WebGPU specification",
      "url": "https://www.w3.org/TR/webgpu/",
      "note": "Normative resource, command, and synchronization model."
    },
    {
      "title": "WGSL specification",
      "url": "https://www.w3.org/TR/WGSL/",
      "note": "Normative shader language and invocation semantics."
    }
  ],
  "checks": [
    "CPU baseline addition runs without GPU discovery",
    "Learner goal checks invoke actual compute and test N=1,64,67,129 with scales0.5 and−2",
    "Ordinary tests pass while recognizing the unfinished scaled-update goal",
    "Explicit --gpu runs real selected WGSL, dispatches and maps output before comparing",
    "Shape/nonfinite/scale validation and GPU buffer/dispatch limits are checked"
  ],
  "limitations": [
    "CPU checks cannot establish WGSL execution or GPU speed",
    "Baseline serial chunking is correct addition but does not implement the parallel scaled-update goal",
    "The hardware route deliberately requires Vulkan and rejects CPU adapters",
    "GPU setup is supplied and pipelines are not tuned per adapter"
  ],
  "redesign_version": 2,
  "steps": [
    {
      "id": "question",
      "title": "What answer should leave the GPU?",
      "session": 1,
      "minutes": 7
    },
    {
      "id": "buffers",
      "title": "How do five numbers become shader inputs?",
      "session": 1,
      "minutes": 8
    },
    {
      "id": "dispatch",
      "title": "Who owns the last three values?",
      "session": 1,
      "minutes": 9
    },
    {
      "id": "vector-build",
      "title": "Build the scaled vector operation",
      "session": 1,
      "minutes": 16
    },
    {
      "id": "adapter",
      "title": "Which device will actually execute it?",
      "session": 2,
      "minutes": 8
    },
    {
      "id": "readback",
      "title": "When is a GPU result safe to read?",
      "session": 2,
      "minutes": 9
    },
    {
      "id": "hardware-build",
      "title": "Verify the real round trip",
      "session": 2,
      "minutes": 18
    },
    {
      "id": "review",
      "title": "Explain the result without the code open",
      "session": 2,
      "minutes": 5
    }
  ],
  "sessions": [
    {
      "id": 1,
      "title": "From serial chunks to independent invocations",
      "goal": "Implement scaled vector arithmetic and guarded per-element WGSL"
    },
    {
      "id": 2,
      "title": "Prove dispatch and synchronized readback",
      "goal": "Run hardware parity or explicitly record unsupported hardware; vary launch geometry"
    }
  ],
  "lab": {
    "package": "s06-gpu",
    "chapter": "29",
    "entry": "labs/s06-gpu/src/ch29.rs",
    "goal": "Implement guarded parallel SAXPY and verify actual hardware output against its CPU oracle",
    "check": "just lab-check 29",
    "manual_checks": [
      "Review the actual ch29.rs shader: one global invocation owns each valid output; workgroup size matches VECTOR_WORKGROUP_SIZE; every input load and output store is bounded; scale bits are decoded.",
      "Trace N=67 and129, including final valid and first invalid IDs; require actual --check --gpu parity/readback before recording hardware completion."
    ]
  },
  "interactives": [
    {
      "id": "gpu-workgroups",
      "title": "GPU workgroups and residency",
      "file": "demo.js",
      "anchor": "gpu-workgroups",
      "illustration": true
    }
  ],
  "topic_coverage": {
    "Adapter discovery": "adapter",
    "wgpu": "adapter",
    "WGSL": "vector-build",
    "buffers": "buffers",
    "dispatch": "dispatch",
    "readback": "readback"
  }
}
