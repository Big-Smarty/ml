{
  "outcomes": ["Validate and parse big-endian IDX image and label files", "Compute stable softmax probabilities and cross-entropy from logits", "Map a dense layer's weights and bias to a ten-class linear model", "Train with mean minibatch gradients", "Separate validation choices from final test evaluation"],
  "lessons": ["IDX files", "Softmax cross-entropy", "Linear classifier training", "Fixture and MNIST modes", "Stable-classification practice", "Review and transfer"],
  "terms": {
    "idx-format": {"name":"IDX format","definition":"A binary array format with magic, dimension sizes, and a contiguous payload.","explanation":"MNIST image IDX files begin with magic number 2051, a count, rows, and columns; label files begin with 2049 and a count. Header integers are 32-bit big-endian values. Payload bytes follow in row-major order. A safe reader validates magic, positive dimensions, matching counts, checked size arithmetic, and exact byte length before interpreting data. The format supplies structure, while dataset meaning and split policy come from MNIST documentation."},
    "softmax": {"name":"Softmax","definition":"A function that converts a vector of logits into positive probabilities summing to one.","explanation":"Softmax exponentiates shifted logits and divides each exponential by their sum. Subtracting the maximum logit prevents overflow without changing the ratio. With cross-entropy and a class-index target, the logit gradient is probability minus the one-hot target. Softmax probabilities express competition among the listed classes; they do not guarantee calibrated uncertainty or account for classes absent from the model. Temperature scaling can change confidence while preserving the same basic normalization."}
  },
  "sources": [
    {"title":"The MNIST database","url":"https://yann.lecun.org/exdb/mnist/","note":"Official counts and IDX byte layout"},
    {"title":"torchvision MNIST loader","url":"https://github.com/pytorch/vision/blob/main/torchvision/datasets/mnist.py","note":"Verified archive source names and checksums used by the preparation tool"},
    {"title":"PyTorch CrossEntropyLoss","url":"https://docs.pytorch.org/docs/stable/generated/torch.nn.CrossEntropyLoss.html","note":"Logits and log-softmax plus NLL"},
    {"title":"Gradient-Based Learning Applied to Document Recognition","url":"https://doi.org/10.1109/5.726791","note":"Foundational handwritten recognition paper"}
  ],
  "checks": ["Generated bytes parse through the real IDX loader", "Malformed magic and payload sizes are rejected", "Fixture mean cross-entropy falls by at least 80 percent", "Fixture held-out accuracy reaches 100 percent", "Explicit MNIST mode requires readable paths", "cross_entropy_from_logits retains ln 10 for ten equal logits at a common offset of 1e16"],
  "limitations": ["The generated fixture covers only three labels and is not a quality benchmark", "The model is linear", "The reference uses deterministic sequential minibatches and no shuffling"]
}
