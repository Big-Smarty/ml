{
  "outcomes": [
    "Trace a tree reduction through workgroup memory and barriers",
    "Derive a tiled row-major matrix multiplication for rectangular matrices",
    "Guard partial tiles without allowing any lane to skip a barrier",
    "Validate dimensions, checked products, device limits, and input lengths",
    "Use Gpu::matmul and Gpu::reduce_sum with their named scalar oracles"
  ],
  "lessons": [
    "Which neighboring values must one result use?",
    "Combine values with a shared reduction tree",
    "Build and trace the staged reduction",
    "Reuse one loaded tile across output owners",
    "Which addresses remain valid at an edge?",
    "Build tiled GEMM and challenge its edges",
    "Defend both synchronization boundaries"
  ],
  "terms": {
    "reduction": {
      "name": "reduction",
      "definition": "An operation that combines many values into fewer values using an associative rule.",
      "explanation": "A sum reduction repeatedly combines values until one subtotal remains. Gpu::reduce_sum maps an input shaped [L] to [ceil(L/512)] partial sums at each stage, while reduce_sum_scalar maps the original [N] directly to one f32. The operation sums all values and does not average them. Addition is associative over real numbers but not exactly associative in floating-point arithmetic, so the staged and scalar accumulation orders may differ in low-order bits."
    },
    "workgroup-memory": {
      "name": "workgroup memory",
      "definition": "Temporary storage shared by invocations in one workgroup for the lifetime of that workgroup.",
      "explanation": "WGSL declares workgroup memory separately from storage buffers. It is suited to reused tiles and partial sums because neighboring lanes can read values loaded by one another. Its visibility is strictly local: another workgroup cannot see it, and it disappears when the workgroup finishes. Hardware limits its capacity, so tile dimensions are a tuned constant rather than an unlimited cache. Correct use requires barriers at ownership handoffs."
    },
    "workgroup-barrier": {
      "name": "workgroup barrier",
      "definition": "A synchronization point that all active invocations in a workgroup must reach in uniform control flow.",
      "explanation": "A workgroup barrier makes earlier workgroup-memory writes visible before later reads proceed. In tiled multiplication, one barrier follows tile loading and another prevents fast lanes overwriting a tile while slow lanes still consume it. The barrier does not synchronize different workgroups; ordered compute-pass boundaries separate global reduction stages. Returning early before a barrier is unsafe, so edge lanes write zero, participate in every barrier, and skip only the final global output store."
    },
    "tile": {
      "name": "tile",
      "definition": "A small rectangular block of a larger array processed cooperatively.",
      "explanation": "The matrix kernel uses 16×16 tiles. A 16×16 workgroup loads one tile from A and one from B, then reuses each loaded element across multiple multiply-adds. The K dimension may require several tile rounds. When dimensions are not multiples of 16, the last round contains invalid positions; those lanes load zero. Tiling changes data movement and order of accumulation while preserving the matrix product within floating-point tolerance."
    },
    "gemm": {
      "name": "GEMM",
      "definition": "General matrix multiplication, usually the operation C = A × B with rectangular dimensions.",
      "explanation": "For A shaped [m,k] and B shaped [k,n], GEMM returns C shaped [m,n]. Each C[row,col] is the dot product of A's row and B's column. Dense neural-network layers depend on this operation, which is why chapter 30 exposes a stable library API for later decoder chapters. The teaching kernel is real but intentionally simple; production libraries add architecture-specific vectorization, packing, and scheduling selected from measurements."
    }
  },
  "sources": [
    {
      "title": "WGSL specification",
      "url": "https://www.w3.org/TR/WGSL/",
      "note": "Normative workgroup memory, barrier, and uniformity rules."
    },
    {
      "title": "WebGPU specification",
      "url": "https://www.w3.org/TR/webgpu/",
      "note": "Normative compute dispatch and resource validation model."
    },
    {
      "title": "wgpu workgroups example",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/examples/features/src/hello_workgroups/README.md",
      "note": "First-party workgroup explanation for v29."
    },
    {
      "title": "wgpu synchronization example",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/examples/features/src/hello_synchronization/README.md",
      "note": "First-party synchronization guidance."
    }
  ],
  "checks": [
    "Direct CPU baseline and completed blocked CPU path agree on nonsquare odd shapes",
    "Staged reduction checks include N=1,600,777",
    "Actual --gpu dispatch verifies odd K, multiple output groups and partial tiles",
    "Numerical checks explicitly do not prove a tree/tiled implementation; learner supplies ownership and uniform-barrier traces",
    "Malformed, zero, overflowing, nonfinite and hardware-limit violations return errors"
  ],
  "limitations": [
    "The correct naive baseline passes parity; a code/trace explanation is needed to establish cooperation",
    "The fixed16×16 tile is educational rather than adapter-tuned",
    "Each matmul call uploads inputs and maps output; only the chapter31 training loop is resident",
    "Floating-point operation order prevents a universal bitwise CPU/GPU guarantee"
  ],
  "redesign_version": 2,
  "steps": [
    {
      "id": "question",
      "title": "Which neighboring values must one result use?",
      "session": 1,
      "minutes": 6
    },
    {
      "id": "reduction",
      "title": "Combine values with a shared reduction tree",
      "session": 1,
      "minutes": 13
    },
    {
      "id": "reduce-build",
      "title": "Build and trace the staged reduction",
      "session": 1,
      "minutes": 21
    },
    {
      "id": "matmul",
      "title": "Reuse one loaded tile across output owners",
      "session": 2,
      "minutes": 12
    },
    {
      "id": "edges",
      "title": "Which addresses remain valid at an edge?",
      "session": 2,
      "minutes": 9
    },
    {
      "id": "tile-build",
      "title": "Build tiled GEMM and challenge its edges",
      "session": 2,
      "minutes": 18
    },
    {
      "id": "review",
      "title": "Defend both synchronization boundaries",
      "session": 2,
      "minutes": 5
    }
  ],
  "sessions": [
    {
      "id": 1,
      "title": "Cooperate on a staged sum",
      "goal": "Implement and trace a real shared-memory reduction with CPU agreement"
    },
    {
      "id": 2,
      "title": "Cooperate on rectangular matrix tiles",
      "goal": "Implement tiled WGSL, explain barriers, and verify multiple odd dimensions"
    }
  ],
  "lab": {
    "package": "s06-gpu",
    "chapter": "30",
    "entry": "labs/s06-gpu/src/ch30.rs",
    "goal": "Build cooperative reduction and tiled GEMM; combine parity with a barrier/ownership explanation",
    "check": "just lab-check 30",
    "manual_checks": [
      "Trace N=600 reduction as 600→2→1 and identify which invocation owns each scratch/subtotal write.",
      "Review actual MATMUL/REDUCE source for cooperative shared loads, halving tree, uniform barriers, no early edge return, and distinct output stores.",
      "Trace the 17×19 by19×33 grid:6 workgroups,2 K rounds, edge zero filling and both barriers; attach real CPU/GPU parity when hardware is supported."
    ]
  },
  "interactives": [],
  "topic_coverage": {
    "Workgroups": "matmul",
    "shared memory": "reduction",
    "barriers": "reduction",
    "edge dimensions": "edges"
  }
}
