{
  "outcomes": [
    "Separate CPU wall-clock latency from device timestamp duration",
    "Explain asynchronous queue submission and explicit completion points",
    "Fuse an affine transform and ReLU while preserving a scalar oracle",
    "Query timestamp and shader-f16 support before requesting optional features",
    "Relate wgpu's f16 feature to concrete Vulkan capability extensions"
  ],
  "lessons": [
    "Do two execution plans compute the same values?",
    "Which clock answers the performance question?",
    "Make the comparison repeatable",
    "Where does asynchronous work become complete?",
    "Build a warmed repeated experiment",
    "Remove one intermediate without changing the answer",
    "Select precision by capability and numerical range",
    "Integrate and measure the completed kernels",
    "Separate capability, correctness, and speed"
  ],
  "terms": {
    "gpu-timestamp": {
      "name": "GPU timestamp",
      "definition": "A device-written counter sampled around GPU work and converted using the queue's timestamp period.",
      "explanation": "Timestamp queries measure intervals on the device timeline. The reference writes a timestamp at the beginning and end of each compute pass, resolves 64-bit ticks into a query buffer, copies them to a mappable staging buffer, and multiplies tick differences by nanoseconds per tick. This excludes some host work and does not equal end-to-end latency. The feature is optional, so the program reports unavailable rather than inventing a duration."
    },
    "wall-clock-latency": {
      "name": "CPU wall-clock latency",
      "definition": "Elapsed CPU-observed time across a precisely stated operation boundary.",
      "explanation": "The project's CPU wall clock begins before bind-group creation and command encoding and ends after output mapping completes. It therefore includes host encoding, queue submission, scheduling, GPU execution, output copying, polling, and output readback, while initial buffer and query allocation are outside the boundary. Mapping and decoding the timestamp staging buffer happen after this boundary. Small operations are noisy, so the experiment warms up and reports a median and range over repeated runs."
    },
    "kernel-fusion": {
      "name": "kernel fusion",
      "definition": "Combining adjacent operations into one kernel so an intermediate need not travel through global memory.",
      "explanation": "Separate affine and ReLU kernels read the input, write an intermediate, read that intermediate, and write output: four storage-value accesses per element. The fused kernel reads input and writes output: two. Fusion also removes one dispatch. These are algorithmic access counts, not measured bus transfers. The chapter measures both GPU plans and checks them against the same `affine_relu_scalar` CPU oracle before drawing any performance conclusion."
    },
    "mixed-precision": {
      "name": "mixed precision",
      "definition": "Using more than one floating-point format to trade range and accuracy for storage or arithmetic benefits.",
      "explanation": "The optional chapter path reads f32 input, converts arithmetic to f16, then writes f32 output. It checks `SHADER_F16`, requests the feature only when supported, and creates the f16 pipeline only after that gate; otherwise the same operation runs in f32. Half precision has a much smaller finite range and fewer significand bits, so finite f32 input is not automatically safe. A separate range guard selects f16 only for the bounded teaching fixture."
    },
    "vulkan-extension": {
      "name": "Vulkan extension",
      "definition": "A named set of capabilities that augments a Vulkan implementation beyond core requirements.",
      "explanation": "At the lower API layer, half arithmetic and half storage are distinct capabilities. `VK_KHR_shader_float16_int8` exposes shaderFloat16 arithmetic, while `VK_KHR_16bit_storage` controls where 16-bit values may be stored. Cooperative matrix extensions add subgroup operations with supported shapes and element types. wgpu normally translates these details into portable features such as `SHADER_F16`; querying before use keeps the program correct across adapters and avoids unsupported shader creation."
    }
  },
  "sources": [
    {
      "title": "wgpu 29 timestamp query example",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/examples/features/src/timestamp_queries/mod.rs",
      "note": "First-party timestamp creation, resolve, and period conversion."
    },
    {
      "title": "wgpu 29 feature definitions",
      "url": "https://github.com/gfx-rs/wgpu/blob/v29/wgpu-types/src/features.rs",
      "note": "First-party TIMESTAMP_QUERY and SHADER_F16 capability definitions."
    },
    {
      "title": "VK_KHR_shader_float16_int8",
      "url": "https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_shader_float16_int8.html",
      "note": "Khronos extension for 16-bit arithmetic."
    },
    {
      "title": "VK_KHR_16bit_storage",
      "url": "https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_16bit_storage.html",
      "note": "Khronos extension for 16-bit storage."
    },
    {
      "title": "VK_KHR_cooperative_matrix",
      "url": "https://docs.vulkan.org/refpages/latest/refpages/source/VK_KHR_cooperative_matrix.html",
      "note": "Khronos cooperative matrix extension."
    }
  ],
  "checks": [
    "CPU baseline prints affine/ReLU output and a one-sample schedule without hardware timing",
    "Actual learner schedule is checked for three warmups and seven alternating measured samples per plan",
    "Every GPU warmup and measured result is checked against CPU affine/ReLU",
    "TIMESTAMP_QUERY and SHADER_F16 are queried and only supported features are requested",
    "The half shader is gated by feature/range and the50,000 boundary fixture explicitly selects f32"
  ],
  "limitations": [
    "The learner baseline is one separate-pass sample, not a completed comparison",
    "CPU schedule checks do not establish WGSL correctness or measured speed",
    "Device pass sums, encoding-to-map wall time and whole-call latency enclose different work",
    "Half arithmetic uses f32 storage, and the range guard is not a universal accuracy guarantee",
    "This microbenchmark does not establish application or training throughput"
  ],
  "redesign_version": 2,
  "steps": [
    {
      "id": "question",
      "title": "Do two execution plans compute the same values?",
      "session": 1,
      "minutes": 6
    },
    {
      "id": "timing",
      "title": "Which clock answers the performance question?",
      "session": 1,
      "minutes": 8
    },
    {
      "id": "measurement-design",
      "title": "Make the comparison repeatable",
      "session": 1,
      "minutes": 8
    },
    {
      "id": "async",
      "title": "Where does asynchronous work become complete?",
      "session": 1,
      "minutes": 5
    },
    {
      "id": "experiment-build",
      "title": "Build a warmed repeated experiment",
      "session": 1,
      "minutes": 14
    },
    {
      "id": "fusion",
      "title": "Remove one intermediate without changing the answer",
      "session": 2,
      "minutes": 9
    },
    {
      "id": "precision",
      "title": "Select precision by capability and numerical range",
      "session": 2,
      "minutes": 12
    },
    {
      "id": "fused-build",
      "title": "Integrate and measure the completed kernels",
      "session": 2,
      "minutes": 17
    },
    {
      "id": "review",
      "title": "Separate capability, correctness, and speed",
      "session": 2,
      "minutes": 5
    }
  ],
  "sessions": [
    {
      "id": 1,
      "title": "Design an honest GPU measurement",
      "goal": "Implement a warmed repeated schedule and explain clock boundaries"
    },
    {
      "id": 2,
      "title": "Fuse and evaluate precision choices",
      "goal": "Implement a fused shader, compare actual results, and interpret measured distributions"
    }
  ],
  "lab": {
    "package": "s06-gpu",
    "chapter": "32",
    "entry": "labs/s06-gpu/src/ch32.rs",
    "goal": "Implement a fused WGSL kernel and a warmed, alternating, correctness-backed hardware experiment",
    "check": "just lab-check 32",
    "manual_checks": [
      "Review actual ch32.rs fused WGSL for bounded affine/ReLU computation with one output store and no storage intermediate.",
      "Require actual --check --gpu for shader parity, optional-feature/range reporting and warmed distributions; CPU schedule checks alone do not establish execution or speed."
    ]
  },
  "interactives": [],
  "topic_coverage": {
    "Asynchronous execution": "async",
    "timing": "timing",
    "fusion": "fusion",
    "mixed precision": "precision",
    "Vulkan extension": "precision"
  }
}
