{
  "outcomes": ["Explain why a linear neuron cannot represent XOR", "Trace a 2-2-1 network forward by hand", "Derive and implement both layers of backpropagation", "Check hidden and output gradients with central differences"],
  "lessons": ["The XOR limit", "Manual chain rule", "Stable objective and checking", "The Rust network", "Backward-pass practice", "Review and transfer"],
  "terms": {
    "hidden-layer": {"name":"Hidden layer","definition":"A layer whose activations are internal features rather than final predictions.","explanation":"A hidden layer receives inputs from an earlier layer and supplies transformed values to a later one. Its parameters are learned through gradients propagated from the loss. When a nonlinear activation lies between dense layers, the model can represent relationships that a single affine transformation cannot. Hidden does not mean inaccessible: during development we can inspect activations and gradients. The term only distinguishes these units from the provided inputs and requested outputs."},
    "backpropagation": {"name":"Backpropagation","definition":"An efficient reverse application of the chain rule through a computation.","explanation":"Backpropagation first evaluates a model and saves intermediate quantities, then sends derivatives from the scalar loss toward earlier parameters. Each operation multiplies an incoming derivative by its local derivative, while shared dependencies add contributions. The method computes the same analytical gradients one could derive symbolically, but reuses intermediate work. Parameters must remain at their forward-pass values until every gradient that depends on them has been computed."},
    "nonlinearity": {"name":"Nonlinearity","definition":"A function that prevents stacked affine layers from collapsing into one affine map.","explanation":"Sigmoid, tanh, and ReLU are common nonlinear activation functions. Without one between dense layers, multiplying and adding through several layers is algebraically equivalent to one weight matrix and bias, so depth adds no representational power. A nonlinearity changes both the forward values and the derivative passed backward. Its saturation or nondifferentiable points can affect optimization, which is why activation choice and initialization are connected."}
  },
  "sources": [
    {"title":"Learning representations by back-propagating errors","url":"https://doi.org/10.1038/323533a0","note":"Original influential backpropagation paper"},
    {"title":"PDP Handbook: Training Hidden Units","url":"https://web.stanford.edu/group/pdplab/pdphandbookV3/handbookch6.html","note":"Explicit XOR derivation"},
    {"title":"Perceptrons","url":"https://mitpress.mit.edu/9780262130431/perceptrons/","note":"Linear perceptron capabilities and limits"}
  ],
  "checks": ["10,000 full-batch updates produce XOR loss below 0.02", "All four XOR labels are classified correctly", "Hidden and output weight gradients match central differences", "Nonpositive learning rates are rejected", "The actual update matches central differences for all nine parameters"],
  "limitations": ["The exact four-row fixture does not establish noisy-data generalization", "Sigmoid hidden units can saturate", "Fixed-size arrays cover only the stated 2-2-1 architecture"]
}
