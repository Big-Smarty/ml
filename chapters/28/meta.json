{
  "outcomes": ["Explain SIMD lanes, vector prefixes, horizontal reduction, and scalar tails", "Distinguish compiler auto-vectorization from an inspected guarantee", "Implement runtime-guarded AVX2 and FMA dispatch with a scalar fallback", "Run an actual stable AVX-512F extension only on supported x86 CPUs", "Document unsafe invariants and test empty, short, and odd vector lengths"],
  "lessons": ["SIMD lanes and tails", "Compiler auto-vectorization", "Safe runtime dispatch", "Stable AVX-512 extension", "SIMD practice", "Review and transfer"],
  "terms": {
    "simd": {"name":"SIMD","definition":"Single instruction, multiple data: one instruction applies an operation across several lanes.","explanation":"A 256-bit AVX register can hold eight f32 values, while a 512-bit AVX-512 register can hold sixteen. Vector kernels load complete groups, compute lane-wise, and often reduce lanes to a scalar. SIMD can increase arithmetic throughput, but memory traffic, reduction overhead, dependencies, and CPU frequency can limit gains. The same executable must retain a safe fallback for architectures or CPUs without the requested instruction set."},
    "scalar-tail": {"name":"Scalar tail","definition":"Elements left after processing every complete SIMD-width group.","explanation":"For length N and lane width W, the tail has N modulo W values. A safe vector loop runs only while at least W elements remain, then ordinary scalar code processes the suffix. This handles empty, short, odd, and exact-multiple lengths without padding or out-of-bounds loads. Masked vector operations are another option on some architectures, but a scalar tail is portable, easy to verify, and sufficient until measurement proves tail cost matters."},
    "auto-vectorization": {"name":"Auto-vectorization","definition":"A compiler transformation from scalar source operations to vector instructions.","explanation":"LLVM’s vectorizers analyze loop legality, dependencies, aliases, target capabilities, and estimated cost. Independent elementwise loops are often easier to transform than floating-point reductions whose reassociation changes rounding. Optimized compilation enables the machinery, but source code has no universal promise that a specific loop vectorizes. Inspect generated code or optimization remarks before making that claim, and benchmark the resulting function with correctness checks and observable outputs."},
    "runtime-feature-dispatch": {"name":"Runtime feature dispatch","definition":"Selecting an implementation after detecting instruction support on the CPU currently running the program.","explanation":"A portable x86 binary can contain scalar and target-feature functions. The generic caller uses is_x86_feature_detected! and invokes an AVX function only when every required capability is present. Architecture cfg gates keep x86 intrinsics out of other targets. The target-feature call is unsafe because executing unsupported instructions is undefined behavior; runtime checks, equal lengths, and proven in-bounds loads form the documented safety argument."}
  },
  "sources": [
    {"title":"Rust Reference target_feature","url":"https://doc.rust-lang.org/stable/reference/attributes/codegen.html","note":"Target-feature calling safety"},
    {"title":"Rust is_x86_feature_detected macro","url":"https://doc.rust-lang.org/stable/std/macro.is_x86_feature_detected.html","note":"Runtime CPU feature detection"},
    {"title":"Rust _mm256_fmadd_ps intrinsic","url":"https://doc.rust-lang.org/core/arch/x86/fn._mm256_fmadd_ps.html","note":"Stable AVX2/FMA operation"},
    {"title":"Rust 1.89 release notes","url":"https://blog.rust-lang.org/2025/08/07/Rust-1.89.0/","note":"AVX-512 stabilization"},
    {"title":"LLVM Auto-Vectorization","url":"https://llvm.org/docs/Vectorizers.html","note":"Loop and SLP vectorizer behavior"}
  ],
  "checks": ["dot_dispatch and dot_avx512_checked reject unequal lengths before any intrinsic call", "dot_dispatch handles every length 0 through 39 and odd length 1003", "dot_scalar and every runtime-supported AVX2/FMA and AVX-512F backend are compared directly with dot_f64_reference", "The auto-vectorization candidate handles an odd length", "Explicit AVX-512 mode returns a clear unsupported error when detection fails", "Every unsafe call and load has documented feature and bounds invariants"],
  "limitations": ["Explicit SIMD implementations cover x86/x86_64 but not ARM NEON", "The project uses scalar tails rather than masked AVX-512 loads", "Default selection prefers AVX2/FMA and does not auto-tune backends", "Auto-vectorization must be inspected for each compiler and target", "No speedup is claimed without local release measurements"]
}
