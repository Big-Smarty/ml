{
  "outcomes": ["Compute convolution output shapes and values with padding and stride", "Trace max-pooling indices and route their gradients", "Backpropagate stable multiclass loss through a dense head and convolution", "Train the tiny digit fixture and optionally load prepared MNIST IDX files"],
  "lessons": ["Why images need shared local weights", "Convolution arithmetic", "Pooling and gradients", "Build and run the classifier", "Practice and debug", "Review and transfer"],
  "terms": {
    "convolution": {"name": "convolution", "definition": "A shared local weighted sum applied across spatial positions.", "explanation": "In neural-network libraries, convolution usually means cross-correlation: the kernel is not flipped before multiplication. A small kernel visits every image position, reusing the same weights, so it can detect a pattern wherever that pattern appears. Local connectivity preserves spatial structure and weight sharing limits the parameter count. A convolution layer may have several learned filters, each producing one output channel. Padding and stride determine which positions are visited and therefore the output shape."},
    "padding": {"name": "padding", "definition": "Extra values conceptually placed around an input before a spatial operation.", "explanation": "Zero padding lets a kernel be centered near an image boundary even though part of its window falls outside the stored pixels. The missing positions contribute zero. With a 3×3 kernel, stride one, and one pixel of padding, output height and width equal the input dimensions. Padding is an architectural choice, not additional observed data. Different padding policies change boundary behavior and must be applied consistently in the forward and backward passes."},
    "stride": {"name": "stride", "definition": "The number of input positions moved between neighboring kernel applications.", "explanation": "Stride one evaluates every adjacent location. Stride two skips every other position and usually reduces spatial resolution. For input size I, kernel size K, padding P, and stride S, the output size is floor((I + 2P − K)/S) + 1 when the numerator is nonnegative. Stride changes both computation and which details are retained. It should not be confused with the memory stride used to index a row-major array."},
    "pooling": {"name": "pooling", "definition": "A fixed reduction over a small spatial neighborhood.", "explanation": "Max pooling keeps the largest activation in each window, while average pooling keeps the mean. Pooling reduces spatial size and makes later layers less sensitive to small local shifts. In backpropagation, average pooling distributes a gradient across every input; max pooling routes it only to the recorded winning position. Exact ties make max pooling nondifferentiable, so gradient checks should use inputs with a unique winner in every tested window."},
    "receptive-field": {"name": "receptive field", "definition": "The input region that can influence a particular activation.", "explanation": "One 3×3 convolutional activation sees a 3×3 image patch. Deeper stacks combine nearby activations, so their effective receptive fields grow even when every kernel remains small. Pooling and stride make the field grow faster by reducing spatial resolution. Receptive field describes possible influence, not proof that every pixel contributes equally after training. It helps connect architecture choices to the scale of patterns a network can represent."}
  },
  "sources": [
    {"title":"Learning representations by back-propagating errors","url":"https://doi.org/10.1038/323533a0","note":"Original concise account of learning hidden representations by backpropagation."},
    {"title":"Neocognitron","url":"https://doi.org/10.1007/BF00344251","note":"Early hierarchical local-feature and position-tolerance architecture."},
    {"title":"Gradient-Based Learning Applied to Document Recognition","url":"https://doi.org/10.1109/5.726791","note":"Primary LeNet account of local receptive fields, shared weights, subsampling, and digit recognition."},
    {"title":"The MNIST Database of Handwritten Digits","url":"https://yann.lecun.org/exdb/mnist/","note":"Official dataset description and IDX format source."}
  ],
  "checks": ["Central-difference and analytic convolution gradients agree away from ReLU and max-pool ties", "Training reduces validation cross-entropy and reaches at least 70% fixture accuracy", "Malformed IDX headers are rejected", "Default execution performs no network access"],
  "limitations": ["The default seven-segment images are course-authored and much simpler than handwriting", "The model has two filters and uses sample-at-a-time SGD", "MNIST mode limits fitting and validation to 2,000 rows for a short CPU demonstration", "No claim is made about competitive MNIST accuracy"]
}
